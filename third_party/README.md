# Third-party repositories

This directory intentionally contains no downloaded repositories in the
initial scaffold. Follow `docs/setup_guide.md` to clone the official Unitree
model sources manually.

Expected layout after setup:

```text
third_party/
├── unitree_ros/
├── unitree_mujoco/
└── model_versions.txt
```

